# Adopt Effect 4 and Effect TSGO

Pi-gentic uses Effect 4 for application workflows, typed failures, service composition, concurrency, resource scopes, and observability, with Effect TSGO as the authoritative compiler diagnostic layer. This beta dependency is pinned and upgraded as one aligned package group because replacing Effect after the runtime refactor would require rewriting every application boundary.
