{
  "name": "pi-gentic",
  "version": "0.4.0",
  "type": "module",
  "description": "Pi extension for orchestrating durable agent sessions, delegation, and worktrees.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CodeByPeete/pi-gentic.git"
  },
  "homepage": "https://github.com/CodeByPeete/pi-gentic#readme",
  "bugs": {
    "url": "https://github.com/CodeByPeete/pi-gentic/issues"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "agents",
    "orchestration",
    "session-management",
    "worktrees"
  ],
  "files": [
    "dist",
    "docs/assets",
    "docs/architecture.md",
    "docs/effect-feature-ledger.md",
    "README.md"
  ],
  "exports": "./dist/extension.js",
  "types": "./dist/extension.d.ts",
  "pi": {
    "extensions": [
      "./dist/extension.js"
    ],
    "image": "https://raw.githubusercontent.com/CodeByPeete/pi-gentic/main/docs/assets/resume.png"
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.js\" \"test-effect/**/*.ts\" \"test-ui/**/*.mjs\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"test/**/*.js\" \"test-effect/**/*.ts\" \"test-ui/**/*.mjs\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:test": "tsc -p tsconfig.test.json",
    "check:effect": "effect-tsgo diagnostics --project tsconfig.json --strict --format pretty",
    "check": "npm run typecheck && npm run typecheck:test && npm run check:effect && npm run lint && npm run check:dead-code && npm test && npm run pack:dry",
    "test": "npm run test:unit && npm run test:effect",
    "test:unit": "npm run build && node --test \"test/**/*.test.js\"",
    "test:effect": "vitest run",
    "test:integration": "npm run build && node --test test/settings/trust.test.js test/worktrees/security.test.js",
    "test:coverage": "npm run build && node --experimental-test-coverage --test-coverage-lines=95 --test-coverage-functions=93 --test-coverage-branches=80 --test-coverage-include=dist/**/*.js --test-coverage-exclude=dist/pi/** --test-coverage-exclude=dist/delegation/runs.js --test-coverage-exclude=dist/extension-runtime.js --test-coverage-exclude=dist/ui/cards.js --test-coverage-exclude=dist/ui/card-renderer.js --test-coverage-exclude=dist/ui/terminal.js --test \"test/**/*.test.js\"",
    "test:coverage:effect": "vitest run --coverage",
    "test:ui": "npm run build && node test-ui/capture-ui.mjs",
    "test:e2e": "npm run build && python test-e2e/capture-terminal-ui.py --deterministic",
    "test:e2e:skills": "npm run build && python test-e2e/capture-terminal-ui.py --skills",
    "test:e2e:live": "npm run build && python test-e2e/capture-terminal-ui.py",
    "prepare": "sh ./scripts/prepare-effect.sh",
    "prepack": "npm run build",
    "pack:dry": "npm pack --dry-run",
    "lint": "oxlint src test test-effect test-ui",
    "check:dead-code": "knip --no-progress --include files,dependencies,unlisted,binaries,exports,types",
    "test:host": "npm run build && node --test test/delegation/active-calls.test.js test/pi/host.test.js test/pi/runtime.test.js test/pi/resume.test.js test/pi/transitions.test.js",
    "test:coverage:host": "npm run build && node --experimental-test-coverage --test-coverage-lines=93 --test-coverage-functions=93 --test-coverage-branches=79 --test-coverage-include=dist/pi/**/*.js --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.84.2",
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@earendil-works/pi-tui": "0.84.2",
    "@effect/tsgo": "0.24.3",
    "@effect/vitest": "4.0.0-beta.101",
    "@types/node": "25.9.3",
    "@vitest/coverage-v8": "4.1.10",
    "knip": "6.32.2",
    "oxlint": "1.78.0",
    "oxlint-tsgolint": "7.0.2001",
    "prettier": "3.8.2",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "0.84.2",
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@earendil-works/pi-tui": "0.84.2"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.101",
    "effect": "4.0.0-beta.101"
  }
}
